services:
  - type: web
    name: link-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT
    envVars:
      - key: BOT_PIN
        value: "1234"
      - key: SECRET_KEY
        generateValue: true
    plan: free
